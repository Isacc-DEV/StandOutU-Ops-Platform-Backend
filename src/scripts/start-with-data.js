#!/usr/bin/env node
import 'dotenv/config';

console.error(
  'start-with-data.js is not yet ported to the PostgreSQL migration. Use npm run seed or port the script to Prisma before running.'
);
process.exit(1);
